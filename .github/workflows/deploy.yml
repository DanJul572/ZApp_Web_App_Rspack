name: Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Create .env file
        run: |
          echo "REACT_APP_API_URL=${{ secrets.ENV_VITE_API_URL }}" >> .env
          echo "REACT_APP_APP_NAME=${{ secrets.ENV_VITE_APP_NAME }}" >> .env
          echo "REACT_APP_THEME=${{ secrets.ENV_VITE_THEME }}" >> .env
          echo "REACT_APP_LANGUAGE=${{ secrets.ENV_VITE_LANGUAGE }}" >> .env
          echo "REACT_APP_ENCRYPTION_KEY=${{ secrets.ENV_VITE_ENCRYPTION_KEY }}" >> .env

      - name: Verify working directory
        run: pwd && ls -la

      - name: Debug .env
        run: |
          echo "===== .env content ====="
          cat .env || echo ".env not found!"
          echo "========================"

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build project
        run: npm run build

      - name: Deploy to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.CPANEL_FTP_SERVER }}
          username: ${{ secrets.CPANEL_FTP_USERNAME }}
          password: ${{ secrets.CPANEL_FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ${{ secrets.CPANEL_FTP_TARGET_DIR }}
